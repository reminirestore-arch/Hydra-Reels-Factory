# 📑 Техническое Задание: Hydra Reels Factory (v2.0)

## 1. Введение

**Hydra Reels Factory** — это профессиональное десктопное приложение для массового производства и уникализации короткого вертикального видеоконтента (Reels/Shorts/TikTok).
Система позволяет загрузить пакет видео, для каждого видео настроить 4 уникальные версии (с разным визуалом и текстом) и автоматически срендерить их.

---

## 2. Архитектура и Стек (Scalable Stack)

* **Core:** Electron (v39+) — доступ к файловой системе, нативные диалоги.
* **UI/Logic:** React 19 + TypeScript — компонентная база, строгая типизация.
* **State Management:** React Context API (на старте) -> **Zustand** (при масштабировании).
* **Graphics:** Fabric.js v6 — манипуляции с оверлеями, текстом, SVG.
* **Video Processing:** FFmpeg (Static Binary) — генерация превью и финальный рендер.
* **Styling:** Tailwind CSS + `clsx` — адаптивный и быстрый UI.

---

## 3. Пользовательский путь (UX Flow)

### Шаг 1: Инициализация (Project Setup)

1. Пользователь выбирает **Input Folder** (папка с исходными `.mp4`/`.mov`).
2. Пользователь выбирает **Output Folder** (куда сохранять результат).
3. Система сканирует Input Folder, фильтрует видеофайлы и **генерирует миниатюры (thumbnails)** для каждого видео (через FFmpeg).

### Шаг 2: Дашборд (Main Interface)

Экран разделен на две части (Master-Detail layout):

* **Левая панель (File Explorer):**
* Скроллящийся список всех найденных видео.
* Каждый элемент списка содержит: Миниатюру, Имя файла, Статус (Готов/Не настроен).


* **Правая панель (Inspector):**
* Отображается только при выборе видео слева.
* Показывает крупное превью выбранного видео.
* **Блок "4 Версии" (Strategies):** 4 крупные кнопки/карточки:
* `IG1: Юмор` (Статус: Текст есть/нет)
* `IG2: POV`
* `IG3: Кликбейт`
* `IG4: ASMR`


* Поле настройки длительности показа текста (Duration).



### Шаг 3: Редактирование (Visual Editor Modal)

1. Пользователь нажимает на одну из 4 кнопок (например, **IG1**).
2. Открывается модальное окно **на весь экран**.
3. **Фон:** Автоматически подгружается первый кадр *конкретно этого видео*.
4. **Инструменты:**
* Добавление/Редактирование текста.
* Настройка подложки (цвет, скругление, прозрачность).
* Перемещение и масштабирование элементов.


5. **Действие:** Кнопка "Сохранить".
* Сохраняет JSON-состояние канвы (для возможности повторного редактирования).
* Сохраняет PNG-файл с прозрачностью (оверлей) во временную папку.
* Закрывает редактор и помечает кнопку IG1 как "Готово".



### Шаг 4: Продакшн (The Factory)

1. Пользователь нажимает кнопку **"ЗАПУСТИТЬ ОБРАБОТКУ"** (внизу дашборда).
2. Система проходит по списку файлов.
3. Для каждой настроенной версии запускает FFmpeg.
4. Показывает общий прогресс-бар и лог операций.

---

## 4. Стратегии Уникализации (FFmpeg Profiles)

Мы переносим проверенную логику из Python-прототипа.

### IG1: Юмор / Классика
* **Концепция:** Легкий кроп и компрессия звука.
* **Видео:** `crop=iw*0.98:ih*0.98:(iw-ow)/2:(ih-oh)/2,scale=1080:1920` (Обрезаем 2% краев и масштабируем обратно).
* **Аудио:** `acompressor=threshold=-12dB:ratio=2:attack=200:release=1000` (Выравнивание громкости).

### IG2: POV / Жиза
* **Концепция:** Ускорение видео и звука (динамика).
* **Видео:** `setpts=0.99*PTS,eq=gamma=1.02` (Ускорение на 1%, повышение гаммы).
* **Аудио:** `atempo=1.01` (Синхронное ускорение).

### IG3: Кликбейт
* **Концепция:** Агрессивная картинка и звук.
* **Видео:** `unsharp=3:3:0.3:3:3:0.0` (Повышение резкости).
* **Аудио:** `anequalizer=c0 f=200 w=100 g=-2 t=1|c0 f=6000 w=1000 g=2 t=0` (V-образный эквалайзер: меньше "гула", больше "верхов").

### IG4: ASMR / Атмосфера
* **Концепция:** Микро-вращение и добавление шума.
* **Видео:** `rotate=0.3*PI/180,scale=1085:1930,crop=1080:1920` (Поворот на 0.3 градуса + зум, чтобы убрать черные углы).
* **Аудио:** Генерация белого шума `anoisesrc` и микширование с оригиналом (`amix`).

---

## 5. Технические детали Рендера (Implementation)

Эти настройки **обязательны** для реализации в TypeScript модуле `src/main/ffmpeg.ts`.

### 5.1. Кодирование
* **Video Codec:** `h264_videotoolbox` (Hardware acceleration for macOS) или `libx264` (fallback для других ОС).
* **Video Bitrate:** `12M` (12 Мбит/с — оптимально для Reels).
* **Audio Codec:** `aac`.
* **Audio Bitrate:** `256k`.
* **Container:** `.mp4`
* **Metadata:** `-map_metadata -1` (Полное удаление метаданных для "чистоты" файла перед алгоритмами соцсетей).

### 5.2. Наложение Оверлея
Если в редакторе создан текст, генерируется PNG.
* Оверлей накладывается **после** видео-фильтров (crop/scale), но **перед** финальным кодированием.

---

## 6. Структура Данных (TypeScript Interfaces)

Это основа того, как мы будем хранить данные внутри приложения.

```typescript
// Описание одной версии (IG1...IG4)
interface VideoStrategy {
  id: 'IG1' | 'IG2' | 'IG3' | 'IG4';
  isReady: boolean;      // Настроен ли оверлей?
  overlayPath?: string;  // Путь к сгенерированному PNG
  canvasState?: object;  // JSON от Fabric.js (чтобы вернуть слои при редактировании)
  textData?: string;     // Сырой текст (для поиска/отображения)
}

// Описание видеофайла
interface VideoFile {
  id: string;            // Уникальный ID (uuid)
  filename: string;      // video_01.mp4
  fullPath: string;      // /Users/user/Desktop/video_01.mp4
  thumbnailPath: string; // /tmp/thumb_video_01.jpg
  duration: number;      // Длительность видео

  // Настройки для конкретного файла
  strategies: {
    IG1: VideoStrategy;
    IG2: VideoStrategy;
    IG3: VideoStrategy;
    IG4: VideoStrategy;
  };

  overlayDuration: number; // Сколько секунд показывать текст (по умолчанию 5)
}

// Глобальное состояние приложения
interface AppState {
  inputPath: string;
  outputPath: string;
  files: VideoFile[];
  selectedFileId: string | null; // Какой файл сейчас открыт справа
  isProcessing: boolean;
}

```

---

## 7. Перспективы развития (Scalability)

1. **База данных (SQLite):** Если файлов станет >1000, хранить всё в памяти (RAM) станет накладно. Мы внедрим `better-sqlite3` для хранения состояния проектов.
2. **Очередь задач (Queue):** Для рендеринга. Позволит ставить на паузу обработку и использовать все ядра процессора (M4 Max) параллельно.
3. **ИИ-генерация:** В будущем подключим OpenAI/Claude API, чтобы текст для IG1/IG2 генерировался автоматически на основе анализа видео.
4. **Шаблоны:** Возможность сохранять не просто "стиль", а целый визуальный шаблон (позиция текста, цвет подложки) и применять его ко всем файлам одним кликом.

---

## 8. Roadmap (План реализации v2.0)

* [x] **Этап 0:** Настройка окружения (Electron + React + TS).
* [x] **Этап 1:** Базовый редактор (Fabric.js + Загрузка фона).
* [x] **Этап 2:** Интеграция FFmpeg (чтение кадров).
* [ ] **Этап 3: Файловый менеджер.**
* Реализовать выбор папки.
* Реализовать сканирование файлов и генерацию миниатюр.
* Верстка списка файлов (Left Panel).


* [ ] **Этап 4: Дашборд (Right Panel).**
* Верстка панели с 4 кнопками.
* Связка данных: Выбор файла -> Обновление правой панели.


* [ ] **Этап 5: Интеграция Редактора.**
* Сделать редактор модальным окном.
* Передача данных: Кнопка IG1 -> Открытие редактора с нужным ID.
* Сохранение результата обратно в стейт файла.


* [ ] **Этап 6: Рендер-движок.**
* Написание генератора команд FFmpeg (TS версия старого Python кода).
* Цикл обработки всех файлов.


* [ ] **Этап 7: Финализация UI.**

---
